namespace NckExchange.Models;

// This model is optional if you only use external login,
// but useful if you ever want to add local username/password login.
public class LoginViewModel
{
    // Properties for local login (if you add them later)
    // public string Email { get; set; }
    // public string Password { get; set; }
}
